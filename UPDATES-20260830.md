# 本轮新增功能总结（2026-08-30）

> 范围：截至今日全部**现存**的新增内容；已被删除或被后续方案替代的旧方案不在列。
> 位置：信箱程序 = `OliviaLetterOverlay`；语音引擎 = `IndexTTS-2.5`（本机）。

## 一、信件朗读（TTS）——从零新增

| 项 | 说明 |
| --- | --- |
| 朗读按钮 | 回信页常驻，点击合成并自动播放；生成中再点=取消，播放中再点=停止；切换信件/关窗自动停止 |
| 重新生成按钮 | 不满意当前语音时，换随机种子重合成一版并覆盖缓存、自动播放 |
| 语音缓存 | 按角色存于 `%LOCALAPPDATA%\OliviaLetterOverlay\characters\<id>\tts\`（默认角色在根目录 `tts\`），重启应用免合成；缓存键含参数指纹（音色路径|停顿|切分|语速，不含种子） |
| 显存策略 | 不做预检拦截，直接尝试显卡生成；失败才报显存不足并弹窗，可当场改选 CPU 慢速（45 秒专用超时不适用，CPU 走 20 分钟上限） |
| 语音后处理 | 自动剥离信末署名（`——`/`—`/`--` 行），纯署名信提示无可朗读内容 |

相关文件：`TtsPreferencesStore.cs`（设置存储）、`IndexTtsClient.cs`（worker 调用/缓存/日志）、`MainWindow.xaml(.cs)`（按钮与状态机）、`ApiSettingsWindow.xaml(.cs)`（设置面板）、`local_tools/olivia_tts_worker.py`（引擎侧隔离 worker：`--text-file/--output/--reference/--seed/--mode/--interval-silence/--max-text-tokens/--duration-factor/--report/--dry-run`，退出码 0/42/1，stdout 输出 PROGRESS 进度）。

## 二、设置界面

- 「信件朗读」**折叠面板**：标题行=启用勾选框；内含自动朗读开关（默认关）、引擎目录（浏览按钮）、参考音色（浏览按钮）、高级参数区（种子/句间停顿 ms/语速倍率 0.3–3.0/切分长度，带「如果你不知道你在改什么，就不要乱改」警示与保存校验）
- 「说话风格学习」卡片：风格观察保留条数（0=一直保存），「清除」按钮删除当前角色全部风格观察
- 设置窗口滚动条改为透明（与主窗口同款隐形样式）
- 引擎目录默认 `D:\codex work\IndexTTS-2.5`，保存时校验 python.exe 与 worker 脚本存在

相关文件：`StylePreferencesStore.cs`（style-preferences.json：保留条数）。

## 三、文字生成「更像人」——提示词与机制

1. **身份钉子**：你不是在扮演林离，你就是林离；禁止星号/括号舞台指示、第三人称自我描述、「作为林离」式表演声明（含正反例）
2. **问答与逻辑底线**：问什么答什么、一次一条思路、不自问自答、前后不矛盾
3. **默认不引用**：不引用来信原话、不复述短语（特殊情况才可引一小句）；显示前机械剥除全部引号字符兜底
4. **语气匹配**：对方轻松就轻松、低落给陪伴不给方案、笑话顺势接住、拒绝委婉；整封最多一条建议
5. **语域钳制**：学生大白话、短句为主、常用字；文艺气质体现在注意到的细节而非修辞；书面腔黑名单（暮色/斑驳/氤氲/温柔地/静静地）、禁「仿佛/像是/宛如」比喻、禁散文化抒情
6. **格式**：书信样子（自然起头 + 「—— 林离」落款），不套任何分段模板
7. **反乱三禁**：不绕回重复、不用生僻字（拿不准就换常用说法）、不连续排比

## 四、随机多样性——反模板机制

- **写法参考**：每封随机抽 1 封（共 10 封内容中性、无落款的样例）只学「说话质感」
- **随机改动增量**：8 种 delta（长度字数化 120–220 / 300–450 字、开头 3 式、结尾 2 式、语气 2 式）抽 2 条不同维度的硬性叠加，与参考冲突以改动为准
- **可选素材**：12 条生活小事池，每封随机 0–2 条，可用可不用
- **跨信去重**：人味段要求避开历史里最近几封的开头、题材、收尾
- 相邻信件形状指令不同、质感 × 改动 × 素材组合，模板感在感知上不可复现

相关文件：`LetterDiversity.cs`（样例与素材池，内容中性以满足角色隔离）、`MimoClient.cs BuildDiversityBlock`。

## 五、回信质量硬校验 + 一次修复重写

- 校验项：星号舞台指示、引号、套话清单（我理解你/别想太多/你真的很棒/相信自己/一切都会好的/加油/总而言之/愿你/在这个）、落款缺失、正文中途角色名、超 1600 字
- 命中即附带具体违规清单重写一次；修复稿问题不多于原稿才采用
- 修复是纯增强：45 秒专用超时 + 全异常兜底，任何失败退回原稿，绝不打断回信
- 全程写 `DiagnosticLog`（ai.quality / ai.style / tts 区域）

相关文件：`LetterQualityCheck.cs`。

## 六、用户说话风格学习——一直学、刻在底层

- 每次寄出信件，后台自动微型分析（96 token）提炼一句「用户说话：…」观察，写入角色记忆
- 保留条数可调（默认 5，0=一直保存），新观察顶旧观察
- 每次回信注入：最近 3 条风格观察保留席位 + 普通记忆补足至 8 条
- 「分析信箱记忆」同步提取说话方式观察，与手动记忆合并去重
- 记忆库中「用户说话：」开头的条目即学习成果，可手动编辑

## 七、生僻字防线

- 提示词：只用最常用字词，拿不准就整句换平常说法
- 机械层 `RareCharGuard.cs`：GB2312 一级常用字（3755 字）白名单判断；已知形近混淆自动纠正（椐→据、捃→拾）；残留生僻字记入日志供持续补映射

## 八、信纸字体

- 全部信纸文字改用「寒蝉手拙体 ChillZhuo v2.500」（随应用输出目录分发，并附官方使用说明）
- 正文保持偏手写的近黑墨色与轻微重画，字体文件缺失时自动回退楷体
- 分页与分离文字层共用同一字体；主窗口和全屏预览保持一致
- 许可注意：按作者仓库说明可免费使用（含商业用途），发布时保留 `Assets\Fonts\ChillZhuo-NOTICE.md` 与作者署名

## 九、其他

- 外层提示词清理：移除全部 4 处顺序令（「自然分段」「先回应再关心」「从第一句开始回应」「先回答后氛围」的固定表述），与内层人设不再打架
- 主动来信指令独立：无信可回时选一个小主题按逻辑说清楚即可
- 测试：回归套件新增质检、风格合并、去署名、缓存指纹、worker dry-run 等用例；当前源码构建通过，但现有回归入口仍会在“固定字数约束”断言处停止（该约束按用户要求暂未改动）

## 十、IndexTTS-2.5 侧工具（本仓库）

- `local_tools/olivia_tts_worker.py`（新增，见第一节）
- `local_tools/run_low_memory_samples.py` 扩展：`--reference-wav / --only / --output-name / --label`（向后兼容，用于参考音频 A/B 与独立结果文件）
- `local_tools/merge_final_report.py`（新增）：合并两轮运行报告为 `final-report.json`，含用户试听决策与复现性验证
- `outputs\low-memory-comparison\final-report.json`：三条核心 WAV + 复验 WAV 的完整证据与试听结论
- `HANDOFF.md` 第 12–17 节：全部决策、参数与注意事项的工程记录

## 十一、遗留注意

- 字体与游戏资源版权：勿公开分发带 `Assets\Fonts` 的程序包
- 显存与游戏互斥：游戏运行时点朗读可能因显存不足失败（有弹窗与 CPU 备选）；后续可加「检测到游戏运行先提醒」
- 引号/生僻字机制为「提示词压概率 + 机械兜底 + 日志积累」，生僻字映射表随日志持续完善

# Olivia Letter 信箱 v1.3.1 更新日志

## v1.3.1 修复版

- 修复 D3D11 动态纹理上传在连续行距下逐行调用 `Marshal.Copy` 的高频开销，改为每帧一次整块复制，非连续行距仍保留安全回退路径。
- 保持现有 Media Foundation + CPU RGB32 播放链路、音频同步、Fade、桌面图标层级和稳定性不变。
- GPU Surface、DXVA、AdvancedVideoProcessing 实验继续关闭，不引入新的硬件解码依赖。
- 更新在线更新器地址和升级包名称至 v1.3.1，用户数据和音乐设置继续保留。

## v1.3 音乐播放重点更新

- 音乐播放链路迁移到 Media Foundation + D3D11，视频帧直接进入桌面壁纸渲染窗口，不再依赖 WMP/EVR 生成独立的全屏视频窗口。
- 音频使用 Media Foundation 解码后通过 WASAPI 输出，视频和声音属于同一个播放实例，支持同步播放、暂停、继续、停止、Seek 和循环。
- 播放启动增加 `Preparing` 阶段：必须先完成解码器初始化、音频缓冲、视频首帧解码并成功呈现，才会启动音频时钟和淡入，避免“有声音但画面卡住”。
- 音频设备时钟作为播放主时钟，视频帧按照 Media Foundation Sample PTS 调度；提前帧等待、迟到帧丢弃和小型帧队列共同减少音画漂移与周期性卡顿。
- 修复开始播放、切换歌曲和 Resume 偶发的启动竞态；旧解码线程会先安全退出，过期回调不会再覆盖新歌曲。
- 修复暂停/继续时音频提前或滞后的问题：淡出期间音画继续同步运行，黑场完成后才一起暂停；Resume 会先准备首帧，再从原位置同时恢复音画。
- 修复播放进度条与实际音频主时钟不一致的问题，Seek 后会清理旧视频帧并重新对齐音频与视频时间轴。
- 保留系统当前音量，不再因为开始播放而把音量突然拉高；静音、循环、上一首/下一首和歌曲自定义名称继续可用。
- 非循环视频结束前会提前淡出，音频停止后恢复原桌面；循环播放不会在循环点重复黑场。
- 新增自包含 `OliviaLetterUpdater.exe`，可在解压包内直接一键升级；单独运行在线版更新器时，会从 GitHub/Gitee 发布页下载升级分卷后再停止旧版、覆盖安装、更新快捷方式并重新启动。
- 桌面图标、WorkerW / SHELLDLL_DefView 层级和普通窗口显示行为保持不变，动态壁纸不使用 TopMost。

## v1.3 发布包

- 提供 `OliviaLetterOverlay-1.3-win-x64-with-updater.7z.001/.002`，Windows 10/11 x64 可直接解压运行。
- 提供 `OliviaLetterOverlay-1.3-upgrade-win-x64-with-updater.7z.001/.002`，覆盖程序文件但保留角色、信件、记忆、API Key 和本地音乐设置。
- 同时提供 `OliviaLetterOverlay-1.3-upgrade-win-x64.zip.001/.002/.003`，供在线更新器下载，所有分卷均小于 100 MB。
- 发布包内含安装、升级、伴随启动和卸载脚本，以及 SHA256 校验文件。

## v1.2 新增与修复

- 重新整理信箱主界面排版：缩短顶部留白，统一邮箱条目对齐，拆分信件标题区和操作区，阅读区域更清晰。
- 保留深色界面与信纸风格，减少重复边框和卡片堆叠；信纸尺寸、分页和文字滚动逻辑不变。
- 新增极简信封线稿应用图标，并同时用于 exe、窗口和任务栏。
- 修复窗口图标资源未正确打包导致发布版启动失败的问题。
- 重新生成 win-x64 自包含发布文件，压缩包根目录包含可直接运行的 `OliviaLetterOverlay.exe`。
- 额外提供 `OliviaLetterOverlay-1.2-upgrade-win-x64.zip` 专用升级包，已有旧版用户无需重新导入本地数据。

## 本次发布

- 增加角色切换与按角色隔离的信件、记忆和设置。
- 增加诊断日志导出、历史聊天记录重命名和 67 封今日写信额度显示。
- 新增本地音乐页：可在设置里选择音乐文件夹，自动识别 `midi` 文件夹及其后面的中文名称；同一首歌的多个视频片段会随机选择播放。
- 音乐通过桌面壁纸方式播放，支持淡入淡出、播放/暂停、上一首/下一首、循环、静音和进度条；音乐列表与信箱页面分开切换。
- 音乐列表支持自定义歌曲名称，播放中的歌曲会显示在播放器栏，不展示内部片段编号。
- 回信预览改为固定纸张/边框/日期、独立文字滚动；全屏预览使用居中等比例叠加布局。
- 回信、寄信、全屏预览和导出信纸统一使用寒蝉手拙体，收发两侧视觉保持一致。
- 写信窗口关闭时会自动保存未完成的草稿，重新打开时恢复；发送成功或点击清空后会删除草稿，且草稿按角色独立保存。
- 记忆库保留数量改为设置项：`-1` 不限量、`0` 不保存、正整数为最多保存条数；回信会注入当前角色保存的全部事实记忆。
- 回信上下文可按最近天数注入：`-1` 不限天数、`0` 不带历史、正整数为最近天数；可设置每 N 封用户来信在后台压缩旧往来并提炼重要记忆，填 `0` 关闭。
- 发布包附带游戏启动器包装、伴随启动和一键还原脚本。

## 修复与增强

- AI 服务类型新增常见服务商预设：OpenAI、Anthropic、Google Gemini、DeepSeek、Moonshot Kimi、智谱 GLM、阿里云百炼 Qwen、火山方舟、xAI Grok、Mistral、Groq、OpenRouter、SiliconFlow、Together AI。
- 各服务商默认地址自动填入，地址仍可修改；自定义 OpenAI 兼容接口和中转站继续可用。
- 模型列表按所选服务地址获取；无上游列表或接口格式不标准时，仍可手动填写模型名。
- API Key 按服务类型分开保存，切换服务商时不会互相覆盖。
- 修复模型名可编辑下拉框出现白底、和暗色界面不统一的问题。
- 强化林离的第一人称身份：来信称呼“林离”时会被理解为称呼回信者，避免把角色当成第三方。
- 保留本地 Ollama 模型管理和常见本地模型预设。

# Olivia Letter 信箱 v1.2 更新日志

## v1.2 新增与修复

- 重新整理信箱主界面排版：缩短顶部留白，统一邮箱条目对齐，拆分信件标题区和操作区，阅读区域更清晰。
- 保留深色界面与信纸风格，减少重复边框和卡片堆叠；信纸尺寸、分页和文字滚动逻辑不变。
- 新增极简信封线稿应用图标，并同时用于 exe、窗口和任务栏。
- 修复窗口图标资源未正确打包导致发布版启动失败的问题。
- 重新生成 win-x64 自包含发布文件，压缩包根目录包含可直接运行的 `OliviaLetterOverlay.exe`。
- 额外提供 `OliviaLetterOverlay-1.2-upgrade-win-x64.zip` 专用升级包，已有旧版用户无需重新导入本地数据。

## 本次发布

- 增加角色切换与按角色隔离的信件、记忆和设置。
- 增加诊断日志导出、历史聊天记录重命名和 67 封今日写信额度显示。
- 回信预览改为固定纸张/边框/日期、独立文字滚动；全屏预览使用居中等比例叠加布局。
- 回信、寄信、全屏预览和导出信纸统一使用寒蝉手拙体，收发两侧视觉保持一致。
- 记忆库保留数量改为设置项：`-1` 不限量、`0` 不保存、正整数为最多保存条数；回信会注入当前角色保存的全部事实记忆。
- 回信上下文可按最近天数注入：`-1` 不限天数、`0` 不带历史、正整数为最近天数；可设置每 N 封用户来信在后台压缩旧往来并提炼重要记忆，填 `0` 关闭。
- 发布包附带游戏启动器包装、伴随启动和一键还原脚本。

## 修复与增强

- AI 服务类型新增常见服务商预设：OpenAI、Anthropic、Google Gemini、DeepSeek、Moonshot Kimi、智谱 GLM、阿里云百炼 Qwen、火山方舟、xAI Grok、Mistral、Groq、OpenRouter、SiliconFlow、Together AI。
- 各服务商默认地址自动填入，地址仍可修改；自定义 OpenAI 兼容接口和中转站继续可用。
- 模型列表按所选服务地址获取；无上游列表或接口格式不标准时，仍可手动填写模型名。
- API Key 按服务类型分开保存，切换服务商时不会互相覆盖。
- 修复模型名可编辑下拉框出现白底、和暗色界面不统一的问题。
- 强化林离的第一人称身份：来信称呼“林离”时会被理解为称呼回信者，避免把角色当成第三方。
- 保留本地 Ollama 模型管理和常见本地模型预设。
